# client-workspaces-hub — FULL BUILD run log

## FOLLOW-UP 2026-07-21 — Asteri campaign filter (Bjion): "only titles containing Navreo", STRICT
Bjion ruled STRICT title matching — the webhook-carrying "Supply Chain US/CA $500M+" (3327088) is deliberately OUT (asked; option declined). Filter = new `workspaces.campaign_filter` (comma-separated ci substrings, NULL=all); asteri='navreo' → **13 campaigns** (ValSoft, PestCo, Alpine ×2, Corp-Dev/Navreo-and-Asteri series, M&A Oct).
- Enforced at 4 seams: server `ws_campaign_ok()` (unified list + scorecard sweep), edge fn **v9** (pending filter; sub-sequences ALWAYS kept — generic names would drop OUR campaigns' positive-reply data), backfill `name_filter`, Make module 60 filter (`lower(1.campaign_name)` contains navreo) — patched + verified live.
- Data cleanup via Management API (MCP timed out mid-run but the migration HAD landed — verify-state lesson again; api.supabase.com/v1/projects/<ref>/database/query with the PAT is the reliable fallback): purged non-matching asteri rows — scorecard 73, versions 25, replies 305, sent_messages 13, contact_history 14,192, campaigns 20, sync_progress 1. Backfill killed + restarted filtered.
- Settings page shows the filter under the workspace name; ADD prompt (page + skill) gained an optional "Campaign filter" line; api_workspaces_add accepts campaign_filter.
- ⚠️ At close: GitHub unreachable (connection resets) — server.py/settings.html filter changes compiled + ready, push pending GitHub recovery. Until the app deploy lands, the OLD live server's hourly scorecard sweep may re-add non-matching asteri scorecard rows — re-run the scorecard cleanup once the filtered build deploys if asteri scorecard count > 13.
Started 2026-07-20. Mode: FULL BUILD. LOOP_TRAINING_MODE: ON (flipped OFF by Bjion after Step 2).

## RUN COMPLETE 2026-07-21 — Step 8 live walk PASSED
Authed browser-pane walk on the live host (cookie-mint injected): campaigns home = **1,010 aggregate rows** (900 navreo + 86 asteri + HR) with INSEAD/ValSoft rows first-class (screenshot); ValSoft detail = full Overview/Leads(2,628)/Sources with LIVE Asteri-key numbers (7,969 sent · 129 replies 1.62% · 31 positives) + optimiser verdict line (screenshot); settings = both workspaces, masked keys, Asteri removable, navreo protected (screenshot). Collective-strip math exact by workspace: navreo 1,142,719/1,706,961/18,177/1,505 + asteri 120,567/145,417/1,930/56. Navreo integrity held all run (796 campaigns / 1,135,987 contacts, organic growth only).
Still converging on autopilot at close: Asteri backfill (~20/qualifying campaigns done, resumable), replies archive (568+), federated mailbox sweep re-kicked after the parallel session's redeploy killed the first run (daily cron covers it regardless — asteri inboxes appear in deliverability when it lands).
WATCH: (1) first organic Asteri reply → `replies` workspace=asteri via the new Make archive; (2) parallel-session campaigns-page redesign (`a12d6c8`) deployed mid-run on top of `da0c21d` — my federation is in the running build; (3) `workspaces.last_sync_at` stamps at the next completed daily tick (v8).

## Step 0 — Preflight + baseline — PASSED
- Repo `~/navreo-signals`: HEAD `a0a8c28`, 0/0 vs origin/main (in sync). Untracked (NOT ours, leave alone): `.claude/`, `app/deliverability-proto.html`, `app/deliverability-tab-proto.js`, `base/`.
- Live host: `/api/version` HTTP 401 headless = up + gated (expected).
- Supabase `fnykldftbkrccihdjayl`: reachable (all baseline queries ran).
- `ASTERI_SMARTLEAD_API_KEY`: present in `~/.navreo-keys.env`; Smartlead GET campaigns = HTTP 200.

### Asteri workspace (their Smartlead, via their key)
- **86 campaigns** (NOT the ~3-4 in the old client memory): 19 ACTIVE, 9 DRAFTED, 50 COMPLETED, 7 ARCHIVED, 1 STOPPED.
- Snapshot: `state/asteri_campaigns_2026-07-20.json` (ids/names/statuses).
- Their campaign IDs (e.g. 3692563) share Smartlead's GLOBAL id sequence with ours — no collision risk on `smartlead_campaign_id` keys, but `workspace` column still required for ownership/purge.

### Supabase baseline (2026-07-20, all rows workspace='navreo')
| table | rows |
|---|---|
| campaigns | 794 |
| campaign_versions | 1,031 |
| contact_history | 1,129,651 |
| replies | 19,104 |
| sent_messages | 25,243 |
| campaign_scorecard | 898 (no workspace col yet) |
| mailbox_stats_daily | 70,672 (no workspace col yet) |

- `sync_progress`: navreo 15 rows + **heyreach 12 rows** — the HeyReach sync shares this ledger under workspace='heyreach'. Do NOT break it; 'heyreach' is a sync-ledger workspace, never a Smartlead workspace.
- Collective-strip totals (sum over campaign_scorecard, 898 rows): **leads 1,143,228 · sent 1,703,128 · replied 18,148 · positives 1,500 · bounced 46,993**.
- Unified-list composition last known: ~898 SL (scorecard sweep = whole account) + 19 HR + drafts.

### Schema reality (changes Step 1's scope — smaller than written)
Already have `workspace`: campaigns, campaign_versions, contact_history, replies, sent_messages, sync_progress.
Missing: `workspaces` table (doesn't exist); `workspace` col on campaign_scorecard + mailbox_stats_daily.

## Step 1 — Schema — PASSED (migration `client_workspaces_federation`)
- `workspaces` table created (RLS on, no policies): id/name/api_key/status/added_at/last_sync_at. Seeded `navreo` row with **api_key NULL by design** — server resolves navreo→env `SMARTLEAD_API_KEY` (no secret duplication); client workspaces resolve from the table.
- `workspace text not null default 'navreo'` added to campaign_scorecard, mailbox_stats_daily, **mailboxes** (discovered federated table: 8,834 rows, PK smartlead_id, FK target of mailbox_stats_daily).
- Done-rule SQL: all 8 tables have the column, zero NULL/blank; counts == baseline on 7 tables; `replies` 19,104→19,109 (+5) = live 3-min reply-sync inserting during the step, all tagged navreo (organic growth, not loss).
- Existing PKs untouched (Smartlead ids are global — no cross-workspace collisions; server upserts keep working).
- Env-file cleanup: `ASTERI_SMARTLEAD_API_KEY` already existed as an `export` line from a prior session; my duplicate append removed, 1 line remains.

## Step 2 — Server federation — PASSED (uncommitted in ~/navreo-signals; deploy = Step 5)
- **Registry + resolver** (server.py, after the sb helpers): `ws_all/ws_enabled/ws_key/ws_for_campaign/ws_key_for_campaign` — 5-min cached `workspaces` reads; navreo key stays env-only; Supabase outage degrades to navreo-only. Campaign→workspace map from campaign_scorecard/campaigns (unknown ids NOT cached, resolve later).
- **`_smartlead_json` auto-derives** the workspace from `/campaigns/<id>/…` paths — every campaign-scoped caller federates without being touched. Explicit `workspace=` for account-scoped calls.
- **Workspace loops**: `_compute_campaigns_unified` (rows now carry workspace + workspace_name; client-ws failure = note, only navreo failure trips last-good) and `_scorecard_sync_all` (rows stamped with workspace).
- **Per-campaign key routing** at direct-URL sites: platform-leads, pause, sequences read, 4 verify/remove job sites, warm lead-count probe, push_to_smartlead, unpush.
- **setter.py**: `_sl_key_for(path)` derives campaign from path via injected `_WS_KEY_FOR_CAMPAIGN` (server injects after configure); `_sl_get`/`_sl_post` both use it. Master-inbox reply-sync loop stays navreo — client reply ingest is Step 7 by design.
- **sync_mailboxes.py**: per-workspace sweep loop, workspace-stamped rows, per-workspace verification; navreo failure = exit 1, client-ws failure = LOUD warning but exit 0 (a revoked client key must not page as fleet failure).
- **API**: GET `/api/workspaces` (masked last-4), POST `/api/workspaces` (validates the key LIVE against Smartlead before storing), POST `/api/workspaces/delete` (typed-name confirm → atomic `rpc/purge_workspace`). Session-gated; `self._post_body` law followed.
- **Fleet-only by design (marked in code)**: outreach destinations, `_deliv_trends_build`, `_fleet_daywise` + presence-check guards. Client lists/pushes stay out of scope.
- **Migrations added**: `purge_workspace_rpc` (single-transaction purge, forbids navreo/heyreach, returns per-table counts) + `workspace_column_indexes` (workspace btree on 7 tables).
- **Gotcha found+fixed**: first purge call timed out client-side but COMMITTED server-side — the 502 "rolled back" report was false. Fix: 300s direct call + state-verify before reporting failure. NEVER report rollback without checking the row.
- **Smoketest evidence**: add smoketest w/ Asteri key → 200, `campaigns_seen: 86`; dup add → 409; list masked (`navreo: env key/898`, `smoketest: …0vlj/0`); delete navreo → 403; wrong confirm → 400; delete → 200 with per-table counts; SQL: zero smoketest residue anywhere; navreo counts untouched (794 / 1,129,651 / 898 / 8,834). Grep done-rule: resolver/env-seed + fallbacks + marked fleet-only sites only. `py_compile` clean on all 3 files.

## Training Mode flipped OFF by Bjion mid-run (after Step 2 report; "Training off") — Steps 3-8 autonomous, done-rules + retry caps kept.

## Step 4 — Settings page — PASSED
- `app/settings.html` (marker `settings-workspaces-v1`) + `shell.js` NAV entry (briefcase icon) = Settings in the rail app-wide.
- Verified rendered on the local mock server (launch config `signals-mock-nobg` in the WORKING DIR's `.claude/launch.json` — the Browser pane reads the working dir's launch.json, NOT the repo's; server boots with `DELIV_MOCK=1 NAVREO_NO_BG=1`, dev-login mints the session).
- **NAVREO_NO_BG=1** added to server `__main__`: HTTP-only boot for local verifies (else a local mock boot would sweep MOCK analytics into the real scorecard and orphan-mark production jobs).
- Evidence: screenshot (navreo `env key`/900 + Smoketest Co `…0vlj`, navreo Remove disabled w/ tooltip); Copy-add-prompt click → success toast (clipboard readback is pane-denied — write path proven by the toast firing after `await writeText`); UI Remove: typed-name gate enables button only on exact match → purge toast reported per-table counts (cron had re-synced 7,398 contacts mid-test — all purged) → SQL residue 0 → row gone. Gotcha: first click landed (0,0) — pane viewport was 0x0; `resize_window` to desktop preset first, then ref clicks work.

## Step 5 — Deploy — PASSED
- Commit `da0c21d` (server.py +327/-98, shell.js, sync_mailboxes.py, settings.html) pushed; Render live in ~3 min.
- ⚠️ Parallel-session event: commit `3e4b867` (setter-uncategorised session) had already swept up my uncommitted setter.py federation edit and pushed it — harmless by design (`_WS_KEY_FOR_CAMPAIGN` defaults None → pre-federation behaviour until server.py landed). Verified my staged diff contained ONLY my hunks before committing.
- Live checks: `/api/version` = da0c21d; settings marker + shell nav served; `/api/workspaces` returns navreo (env key) + asteri (…0vlj); fresh boot healthy.
- **Cookie-mint recipe works** (deliverability-perf-pass memory): `email|exp` HMAC-SHA256 keyed sha256(SRK+":navreo-session-v1"), cookie `navreo_session=<b64url>.<hexsig>` → authed curl. GOTCHA: env-file values are QUOTED — strip quotes when cutting from shell (`tr -d '\"'`), or Management API returns "JWT could not be decoded".

## Step 6 — ADD Asteri — largely PASSED (sweeps still converging)
- Workspace registered via the real add handler (validated live, 86 campaigns seen; clients row pre-existed, untouched).
- **Unified list LIVE: 900 navreo + 86 asteri + 20 HR rows, zero workspace errors** — Asteri campaigns render on top of the aggregate (INSEAD/PE names verified).
- **Live key auto-derivation proven on Render**: `/api/campaign-readonly?id=3396411` (ValSoft, Asteri-owned) returned THEIR real analytics (7,808 sent / 127 replied / 31 positives / 2,628 leads); `/api/campaign-platform-leads` returned their real leads. No code told it whose campaign this is — the resolver derived it.
- Backfill `--workspace asteri` running (full history — made `min_created` per-workspace: navreo keeps the 2026-01-01 economics ruling, asteri takes ALL history for suppression/recontact value). 11+ campaigns archived and climbing. NOTE: script stdout buffers when backgrounded — watch `db/state/backfill_asteri.json` instead.
- Scorecard sweep + federated mailbox sync mid-run (contend on OUR key's 200/min with each other — expect ~15-30 min to fully converge; asteri tiles + inboxes land then).

## Step 7 — Automations pass — PASSED (one watch item)
- **Categoriser 9187631**: was ALREADY live + keyed (memory's "pending token" was stale — the key was in the scenario since June). Real gaps found + fixed via Make REST PATCH (MAKE_API_TOKEN in env file works; blueprint root {name,flow,metadata}, scheduling separate, 85KB payload from file):
  1. **Archive branch added** (new module 60, byte-clone of Navreo's proven module, workspace=asteri + client_id=asteri → ingest). The ingest function needed ZERO changes — it already accepts `workspace` and stamps replies + contact_history. Round-trip verified live: modules [1,29,2,3,4,5,60,6], gate + archive correct, scenario active/valid.
  2. **Cross-campaign gate bug fixed**: Asteri had the July-10 `max(map(...))` "fail closed" gate (lastEdit 2026-07-10) but NEVER got Navreo's July-13 per-campaign fix — with their heavily-reused prospect lists, any lead categorised in an old campaign would silently block categorisation in a new one. Applied Navreo's proven expression verbatim.
  - Empirical pre-check: their 12 newest replies (through 2026-07-20) ALL categorised — fresh-lead flow works; the bug only bit re-contacted leads.
  - Test: direct ingest POST workspace=asteri → replies row tagged asteri/asteri → deleted (synthetic). Full Make-path synthetic replay is IMPOSSIBLE by design (fail-closed gate + stopIfEmpty stop synthetic payloads; a real-lead replay would write real categories). **WATCH: first organic Asteri reply should appear in `replies` with workspace=asteri.**
- **Positive routing 9414775**: webhook (`hook.eu2.make.com/mi39u1ax…`) verified present on exactly the 4 Navreo-run actives (ValSoft, Supply Chain, PestCo, Alpine) — correct scope; their 15 self-run actives use the workspace-level categoriser hook by design. RE-REGISTER law stands for future Navreo-run campaigns.
- **Reply-sync backstop**: stays navreo-only BY DESIGN (setter master-inbox pull). Asteri replies reach the hub via the Make archive path just wired. Positive-card notify for asteri campaigns NOT wired (follow-up candidate).
- **Edge function v8**: workspace-tick completion now stamps `workspaces.last_sync_at` (settings Last-sync column). verify_jwt reset on BOTH deploys (v7, v8) — re-PATCHed false each time, confirmed.

## Step 3 — Sync federation — PASSED (pending final race re-check)
- **Done-rule evidence**: manual tick (pg_net request 12909, same Vault call as cron) + cron continuation synced **8 Asteri campaigns, 7,197 contact_history rows, 8 sequence snapshots — all tagged workspace='smoketest'** (real campaign names verified: Supply Chain US/CA, ValSoft B2B SaaS, Past Work 2 – Pavement…); `sync_progress` row (smoketest, today) created with total=19 = exactly Asteri's ACTIVE campaigns after the standing filter. navreo counts unchanged throughout (794/1,031/898/8,834; contact_history 1,129,651). heyreach ledger untouched (62/62).
- Purge after test: one transaction deleted everything + the workspace-created clients row (conditional cleanup fired); immediate residue = 0. Tick response never lands in pg_net when the function hits wall-clock mid-leads — state, not response, is the verification surface.
- `db/backfill_smartlead.py` needed ZERO changes — it already defines `asteri` with `key_env: ASTERI_SMARTLEAD_API_KEY` (original data-layer build anticipated this; explains the pre-existing env line).
- `clients` table has `smartlead_key_env` column; **asteri already a client**. campaigns.client_id FKs clients → `api_workspaces_add` now upserts a clients row (ignore-duplicates: pre-existing client rows never touched). Purge rpc v2: conditionally deletes a workspace-created clients row ONLY if unreferenced (a DFY client with navreo-era history survives its workspace purge).
- Edge function `smartlead-daily-sync` federated → **deployed v7**: `loadWorkspaces()` from the table (navreo key stays env-only; registry outage degrades to old navreo-only behaviour); per-workspace context loop in added_at order (navreo gets budget first); per-ws `sync_progress` rows; response = per-workspace status map. Mirror updated at `db/smartlead_daily_sync.ts`.
- ⚠️ Law reconfirmed: the deploy DID reset `verify_jwt` to true — PATCHed back to false, confirmed ACTIVE v7 false.
- Gotcha: `SUPABASE_ACCESS_TOKEN` in `~/.navreo-keys.env` is QUOTED — shell `cut` kept the quotes → "JWT could not be decoded". Strip quotes (`tr -d '\"'`) when using it via shell (server's load_keys already strips).
- pg_cron `smartlead-daily-sync-tick` intact (*/5, active, 120s timeout). Manual tick fired via the same Vault-based pg_net call (request 12909) with `smoketest` workspace registered (Asteri key, 86 campaigns seen).

### Notes
- Local `HEYREACH_API_KEY` is STALE (401 Invalid API key). Out of scope (HeyReach untouched; Render has its own env) — flagged to Bjion.
- Step 6 comparison must be per-workspace (fresh navreo count + fresh asteri count at verify time), not raw total vs today's total — the team creates campaigns daily, raw drift would false-fail. Today's numbers above are the recorded "before" picture.
